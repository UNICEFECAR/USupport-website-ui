import React from 'react';

import { WeHelp } from './WeHelp';

export default {
  title: 'Website UI/blocks/WeHelp',
  component: WeHelp,
  argTypes: {},
};

const Template = (props) => <WeHelp {...props} />;

export const Default = Template.bind({});
Default.args = {};
